# vim

Vim/Neovim configuration.

## Version requirements

- Vim 8.0 or higher, or any Neovim

## Plugin manager

[`vim-plug`](https://github.com/junegunn/vim-plug) is bundled at
`.vim/autoload/plug.vim`. Install plugins with `:PlugInstall` after first
launch.
